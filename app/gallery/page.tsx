import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Photo Gallery | Safe & Sound Security",
  description: "View security camera and alarm system installations by Safe & Sound Security.",
};

export { default } from "./GalleryClient";
